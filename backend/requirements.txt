# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.2

# Configuration
pydantic-settings==2.9.1
python-dotenv==1.1.0

# File uploads
python-multipart==0.0.20

# Auth (JWT validation for Auth0)
python-jose[cryptography]==3.4.0

# HTTP client (async calls to OpenAI)
httpx==0.27.2

# MillenniumDB — driver oficial (WebSocket)
millenniumdb-driver

# Wukong — motor de grafos de conocimiento (paquete local)
# Se instala por separado: pip install ./wukong-engine
# (requiere el submodule clonado, ver README)

# Testing
pytest==8.3.5
pytest-asyncio==0.26.0

# Supabase
supabase==2.10.0

# PDF text extraction
pymupdf==1.27.2.3

# Linting
ruff==0.11.6
